import React from 'react';
import { Title as TitleStyled } from './styles/ProductDetailStyles';

const Title = ({ text }) => {
  return <TitleStyled>{text}</TitleStyled>;
};

export default Title;
