import React from "react";
import Button from "../../atoms/login/Button";

export const FindPWBtn = (props) => {
    return <Button {...props}>비밀번호 찾기</Button>;
};

export const RegisterBtn = (props) => {
    return <Button {...props}>회원가입</Button>;
};
