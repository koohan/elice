import styled from "styled-components";

const PaymentOptions = styled.div`
  display: flex;
  gap: 16px;
`;

const OptionWrapper = styled.div`
  flex: 1;
  flex-direction: column;
`;

export { PaymentOptions, OptionWrapper };
